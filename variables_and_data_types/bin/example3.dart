void main(List<String> args) {
  /* var avg = [0.0, 1, 12];
  print(avg);

  avg = [1, 2, 3];
  print(avg);

  var firstAvg = avg.first;
  print(firstAvg); */

  ///نوع داده ادرس از نوع var هست و این نوع داده قابلیت تغییر پذیری هم در زمان کامپایل و هم در زمان اجرا رو دارد
  ///یعنی هم میتونیم مقدار جدیدی به متغییر اختصاص بدیم و همم میتونیم مقدار داخلی متغییر رو تغییر بدیم
  /* var address = "123  Main Street";
  print(address);

  address = "456 Main Street";
  print(address);

  address = address.replaceAll("Main", "Bla");
  print(address); */

  var address = [1, 2, 3];
  print(address);

  address = [1, 2, 3, 4, 5, 6];
  print(address);

  address.add(1200);
  print(address);
}
